{
  "name": "chickadee-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Read articles aloud with Kokoro-82M running entirely in the browser (WebGPU). No server, no API key.",
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "kokoro-js": "^1.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
